// const cloud = require("wx-server-sdk");

// cloud.init();

// const db = cloud.database();
// const _ = db.command;

exports.main = async (event, context) => {
  // const now = db.serverDate();
  // const beginOfToday = now - now % (24 * 60 * 60 * 1000);

  // try {
  //   await db
  //     .collection("userTasks")
  //     .where({
  //       created: _.lt(beginOfToday),
  //     })
  //     .remove();
  // } catch (error) {
  //   console.error(error);
  // }
};
